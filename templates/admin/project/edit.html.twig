{% extends 'container.html.twig' %}

{% block title %}Projects{% endblock %}

{% block container %}
    <h1>Project edit</h1>

    {{ include('admin/project/_form.html.twig', {'button_label': 'Update'}) }}

    {# <div>
        {{ form_start(formProjectfiles) }}
            {{ form_widget(formProjectfiles) }}
            <button>save</button>
        {{ form_end(formProjectfiles) }}

    </div> #}
    <a href="{{ path('project_index') }}">back to list</a>

    {{ include('admin/project/_delete_form.html.twig') }}

{% endblock %}
